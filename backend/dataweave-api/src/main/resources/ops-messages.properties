# DataWeave Ops Agent i18n messages — Chinese (zh-CN)
# Stream C: AG-UI markdown / MCP tool descriptions

# ─── AG-UI markdown ──────────────────────────
ops.agent.failed_instances=以下为最近失败的实例：
ops.agent.no_failed=当前没有失败实例
ops.agent.rerun_suggestion=建议重跑以下实例：
ops.agent.backfill_suggestion=建议对以下日期范围执行补数据：

# ─── MCP 工具描述 ─────────────────────────────
mcp.query_failed_instances=查询最近失败的周期实例（可选按 taskId 过滤）
mcp.rerun_instance=重跑一个失败的任务实例（经策略闸门）
mcp.set_instance_success=将任务实例标记为成功（经策略闸门）
mcp.batch_instance_ops=批量操作实例：rerun/kill/set-success（经策略闸门）
mcp.freeze_task=冻结/解冻任务定义（经策略闸门）
mcp.submit_backfill=提交补数据：对指定日期区间生成补数据实例（经策略闸门）
mcp.query_backfill=查询补数据运行记录

# ─── 审批理由 ────────────────────────────────
ops.approval.rerun=重跑实例
ops.approval.set_success=置成功实例
ops.approval.batch_{0}=批量{0}操作
ops.approval.backfill=补数据
ops.approval.freeze=冻结任务
ops.approval.freeze_node=冻结/解冻节点
