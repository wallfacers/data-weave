=== 062 实时任务运维 — savepoint 端到端真跑取证（TR）===
环境：真实 Flink 1.20.5 docker 集群（dw062-flink-jm/tm，JM REST :8083，JM/TM 共享 savepoint 卷）
      + host flink 客户端（JDK11）+ backend all-in-one（h2 profile）+ dw CLI 服务端 TEST run
结果：PASS=24 FAIL=0（exit=0）

全链路真跑证据（服务端产品路径，非夹具）：
  ① dw push(longRunning:true) → task_def.long_running=TRUE                      ✅
  ② dw run --test → triggerTestRun 物化 instance.long_running=TRUE → 下发       ✅
  ③ worker FlinkTaskExecutor detached 提交 → 真 JobID → 句柄回写实例             ✅（依赖 D1 修复）
  ④ POST /streaming-tasks/{id}/stop → HttpFlinkSavepointClient 真连 Flink REST
     stop-with-savepoint → 轮询 COMPLETED → location=file:/savepoints/savepoint-2e0517-b0dd632dc10a ✅
     → task_checkpoint 记录 SUCCESS/resumable → CAS 实例 STOPPED                 ✅
  ⑤ GET /streaming-tasks/{id}/checkpoints → 检查点真实落库(SUCCESS/resumable)    ✅
  ⑥ Flink REST 侧作业终态 FINISHED（savepoint 优雅停止，非 cancel）             ✅
  ⑦ POST /streaming-tasks/{id}/resume → CAS 转出 STOPPED + resume_checkpoint_id
     落库 + 保留 external_job_handle                                            ✅
  ⑧ D2 续跑经 savepoint 提交全新 Flink 作业（新 JobID a9bc0757… ≠ 旧 2e051707…）✅
  ⑨ D2 决定性：新作业 Flink checkpoint config restored-from =
     file:/savepoints/savepoint-2e0517-b0dd632dc10a（旧作业 stop 时的 savepoint）——
     新作业真从 savepoint 恢复计算状态，而非 reattach 旧终态作业                 ✅

真跑暴露并修复缺陷：
  D1（已修 InProcessTaskExecutionGateway）：all-in-one/in-process 下发路径
     executor.execute 前未 CurrentExecution.bind(taskInstanceId) → Flink long_running
     detached 提交后 external_job_handle 无法回写（writeHandle 取不到实例 id）。
     distributed 路径（WorkerExecService.submit）已绑定，两路径此前不对齐。
     修复：in-process 网关 execute 前后 bind/clear，与 distributed 对齐。
     回归测试 InProcessTaskExecutionGatewayBindTest。

  D2（已修 FlinkTaskExecutor + 下发链）：resumeFromCheckpoint 记录 resume_checkpoint_id
     但执行器此前无 savepoint 恢复逻辑，续跑仅 reattach 旧作业（旧作业已 FINISHED → 判 SUCCESS
     而不恢复状态）。修复：下发链传播 savepoint 路径（SchedulerKernel claim 关联 task_checkpoint
     → DispatchCommand.resumeSavepointPath → EngineSubmitRef.savepointRestorePath → 两 gateway
     → WorkerExecController）；FlinkTaskExecutor savepoint 恢复优先于 reattach（sql 前置
     SET 'execution.savepoint.path'，jar 加 flink run -s <path> -n）；句柄回写清 resume_checkpoint_id
     （消费后 infra-redispatch reattach 新作业）。单测 FlinkTaskExecutorTest（buildCommand -s / sql 注入 /
     EngineSubmitRef 向后兼容），端到端 restored-from 决定性证据见上 ⑨。
