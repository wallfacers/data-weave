env {
  parallelism = 1
  job.mode = "BATCH"
}

source {
  Jdbc {
    url = "jdbc:mysql://host.docker.internal:3307/dataweave_biz?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC"
    driver = "com.mysql.cj.jdbc.Driver"
    user = "biz"
    password = "biz"
    query = "SELECT id, name, val FROM nonexistent_table_xyz"
    result_table_name = "bad_source"
  }
}

sink {
  Console {
    source_table_name = "bad_source"
  }
}
