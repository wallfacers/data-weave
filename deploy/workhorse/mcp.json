{
  "_comment": "workhorse MCP host 配置（schema 对齐 workhorse internal/mcp.host）：接入 DataWeave 平台 MCP Streamable HTTP 端点。auth_header 的 Bearer token 须与 dataweave-api 的 mcp.auth.token 一致（部署时统一覆盖默认值，勿用此处占位值上生产）。",
  "servers": [
    {
      "name": "dataweave",
      "enabled": true,
      "transport": "http",
      "url": "http://127.0.0.1:8000/mcp",
      "auth_header": "Bearer dataweave-local-mcp-token",
      "always_load": true
    }
  ]
}
